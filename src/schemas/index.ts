import post from './post';
import project from './project';
import tag from './tag';
import talk from './talk';
import work, { videoBlock } from './work';

export const schemaTypes = [post, project, tag, talk, work, videoBlock];
