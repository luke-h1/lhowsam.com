name: Nightly unit tests
on: 
  schedule:
    - cron: '0 19 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm i 
    - name: Run tests
      run: npx jest 